# Prometheus 抓取配置：抓取 bookadmin API 实例的 /metrics
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "bookadmin-api"
    static_configs:
      - targets: ["api1:8888", "api2:8888", "api3:8888"]
    metrics_path: /metrics
